import { join } from 'path';

export const VIEWS_PATH = join(process.cwd(), 'dist/apps/my-beautiful-ui/browser');
export const COMPILED_SSR_BUNDLE = join(process.cwd(), 'dist/apps/my-beautiful-ui/ssr/main.js')
